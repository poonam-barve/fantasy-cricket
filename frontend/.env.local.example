# Copy this file to `frontend/.env.local` and edit values for local browser testing.

# Force compact/mobile layout in the browser.
# You can still override it per page with `?mobile=1` or `?compact=1`.
VITE_MOBILE_MODE=0
